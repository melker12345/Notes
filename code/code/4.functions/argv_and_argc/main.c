#include <stdio.h>

int main(int argc, char *argv[]) {
    // TODO: Cover the following topics for "argv and argc":
    // - argc: argument count (number of command-line arguments)
    // - argv: argument vector (array of strings)
    // - argv[0] is program name
    // - Parsing command-line arguments
    // - Converting string arguments to numbers (atoi, atof, strtol)
    // - Common use cases (options, flags, file paths)
    // - Common pitfalls: not checking argc before accessing argv
    // - Common pitfalls: buffer overflows when processing arguments
    return 0;
}

