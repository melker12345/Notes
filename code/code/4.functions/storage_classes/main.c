#include <stdio.h>

int main(void) {
    // TODO: Cover the following topics for "storage classes":
    // - auto (default for local variables)
    // - static (persistent local variables, file-scope variables)
    // - extern (external linkage, forward declarations)
    // - register (hint to store in CPU register)
    // - Storage duration (automatic, static, allocated)
    // - Linkage (internal, external, none)
    // - Scope vs storage duration vs linkage
    // - Common pitfalls: misunderstanding static behavior
    return 0;
}

