#include <stdio.h>

int main(void) {
    // TODO: Cover the following topics for "relational operators":
    // - Equal to (==), not equal to (!=)
    // - Less than (<), greater than (>)
    // - Less than or equal (<=), greater than or equal (>=)
    // - Return values (0 for false, 1 for true)
    // - Comparing different types
    // - Floating-point comparison issues
    // - Common pitfalls: using = instead of ==
    // - Common pitfalls: comparing floating-point numbers for equality
    return 0;
}

