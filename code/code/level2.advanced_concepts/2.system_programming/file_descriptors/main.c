#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

int main(void) {
    // TODO: Cover the following topics for "file descriptors":
    // - File descriptor concept
    // - Standard file descriptors (0=stdin, 1=stdout, 2=stderr)
    // - open(), close(), read(), write()
    // - File descriptor limits
    // - Duplicating file descriptors (dup, dup2)
    // - Common pitfalls: file descriptor leaks
    // - Common pitfalls: not closing file descriptors
    return 0;
}

