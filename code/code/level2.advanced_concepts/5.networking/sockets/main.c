#include <stdio.h>
#include <sys/socket.h>
#include <netinet/in.h>

int main(void) {
    // TODO: Cover the following topics for "sockets":
    // - Socket creation (socket())
    // - Socket types (SOCK_STREAM, SOCK_DGRAM)
    // - Address families (AF_INET, AF_INET6, AF_UNIX)
    // - Socket addresses (sockaddr, sockaddr_in)
    // - Binding and listening
    // - Accepting connections
    // - Common pitfalls: not checking return values
    // - Common pitfalls: address reuse issues
    return 0;
}

