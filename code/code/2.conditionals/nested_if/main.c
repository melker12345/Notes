#include <stdio.h>

int main(void) {
    // TODO: Cover the following topics for "nested if":
    // - Definition: placing an if (or if-else) inside another if block
    // - When nested if statements are useful (multi-criteria checks)
    // - Readability concerns (indentation, braces, early returns)
    // - Using logical operators (&&, ||) to flatten nesting when appropriate
    // - Handling edge cases and default branches to avoid missing paths
    // - Common bugs: forgetting else pairs, unreachable code, indentation errors
    // - Refactoring tips: extract helper functions to reduce depth
    // - Testing strategies to cover every combination of nested conditions
    return 0;
}
