#include <stdio.h>

int main() {
    // TODO: Cover the following topics:
    // 1. What is scope? (where a variable is visible/accessible)
    // 2. What is lifetime? (how long a variable exists in memory)
    // 3. Block scope (local variables)
    // 4. File scope (global variables)
    // 5. Function scope
    // 6. Scope vs lifetime differences
    // 7. Shadowing (inner scope variables hiding outer scope)
    // 8. Common bugs: using variables out of scope, lifetime issues
    
    return 0;
}

