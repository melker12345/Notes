#include <stdio.h>

int main() {
    // TODO: Cover the following topics:
    // 1. What is static? (storage class specifier)
    // 2. Static local variables (persist across function calls, initialized once)
    // 3. Static global variables (internal linkage, file scope)
    // 4. Static functions (internal linkage)
    // 5. Difference between static and non-static variables
    // 6. Common bugs: assuming static variables reset, initialization issues
    
    return 0;
}
