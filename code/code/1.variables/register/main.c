#include <stdio.h>

int main() {
    // TODO: Cover the following topics:
    // 1. What is register? (storage class, hint to compiler)
    // 2. register variable declaration
    // 3. When to use register (frequently accessed variables)
    // 4. Limitations (cannot take address with &)
    // 5. Modern compiler optimizations (register is often ignored)
    // 6. Common bugs: trying to get address of register variable
    
    return 0;
}

